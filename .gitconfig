[user]
name = James Bjorkman
email = james@bjorkman.ca
signingkey = 350117BEA90DB34D

[push]
default = current

[alias]
co = checkout
lg = log --pretty=format:'%C(yellow)%h %C(blue)%<(15,trunc)%an %C(red)%m%G? %C(green)%ci %C(white)%s%C(reset)'
delete = branch -D
del = branch -D
rename = branch -m
new = checkout -b
branch = branch -vv

[core]
excludesfile = ~/.gitignore_global
editor = vim

[credential]
helper = cache

; [commit]
; gpgsign = true
