[user]
  name = James Bjorkman
  email = james@bjorkman.ca

[push]
  default = current

[alias]
  co = checkout
  lg = log --pretty=format:'%C(yellow)%h%C(red)%d %C(blue)%an %C(white)%s %C(reset)- %ar%C(reset)'
  delete = branch -D
  del = branch -D
  rename = branch -m
  new = checkout -b
  branch = branch -vv

[core]
  excludesfile = ~/.gitignore_global
  editor = vim
[credential]
	helper = cache
